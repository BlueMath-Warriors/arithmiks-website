// The "A" logo mark as a vector (36x40), served from /static so its internal
// gradient id can't collide when several copies are on one page.
export const LOGO_MARK_SRC = "/brand/arithmiks-mark.svg";
